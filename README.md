# Multigate KLayout layouts

- `MLA/` contains the original MLA layout files restored from the initial baseline commit.
- `EBL/` contains the new EBL layout revision and its generation scripts.
- `Multigate/` contains the existing microscope image metadata and raw image files.
